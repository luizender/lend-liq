# Development dependencies, pinned for reproducible installs.
# Runtime deps are in requirements.txt; install both for a dev environment:
#   pip install -r requirements.txt -r requirements-dev.txt
# The same tools are declared as the project's "dev" extra:
#   pip install -e ".[dev]"
pytest==9.0.3
pytest-cov==7.1.0
coverage[toml]==7.14.1
pylint==4.0.5
